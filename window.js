window.console.log("This is window");

let wind;
// input = prompt("Enter your name please");
// alert(`Hello ${input}`);

// outer Height and width of the window.
wind = window.outerHeight;
wind = window.outerWidth;

// inner Height and width of the window.
wind = window.innerHeight;
wind = window.innerWidth;

// Scroll Points.
wind = window.scrollX;

// Location object.

wind = window.location.host;

// redirect.
// wind = window.location.href = "https://google.com";

// reload;
wind = window.location.reload;

wind = window.history.go(-1);
wind = window.history.length;

// Navigator object.
wind = window.navigator;
wind = window.navigator.appVersion;
wind = window.navigator.userAgent;
wind = window.navigator.platform;
wind = window.navigator.vendor;
wind = window.navigator.language;
wind = window;

console.log(wind);